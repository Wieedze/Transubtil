<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Merchandising - Transubtil Rec</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f4;
      color: #333;
      margin: 0;
      padding: 0;
    }

    header {
      background-color: #333;
      color: white;
      text-align: center;
      padding: 20px;
    }

    h1 {
      font-size: 2rem;
    }

    .container {
      width: 90%;
      margin: 20px auto;
      padding: 20px;
      background-color: white;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
    }

    .product-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); /* Grille responsive */
      gap: 20px;
      padding: 20px 0;
    }

    .product-card {
      background-color: #fff;
      padding: 20px;
      text-align: center;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      border-radius: 8px;
      transition: transform 0.3s ease-in-out, box-shadow 0.3s ease-in-out;
    }

    .product-card:hover {
      transform: scale(1.05); /* Zoom au survol */
      box-shadow: 0 8px 15px rgba(0, 0, 0, 0.2);
    }

    .product-card img {
      max-width: 80%;
      height: auto;
      border-radius: 8px;
      transition: transform 0.3s ease-in-out;
    }

    .product-card img:hover {
      transform: scale(1.1); /* Animation d'agrandissement de l'image */
    }

    .product-card h3 {
      font-size: 1.4rem;
      margin-top: 10px;
    }

    .product-card p {
      font-size: 1rem;
      color: #666;
    }

    .product-card .price {
      font-size: 1.2rem;
      color: #333;
      margin-top: 10px;
    }

    .product-card a {
      display: inline-block;
      margin-top: 15px;
      padding: 10px 20px;
      background-color: #333;
      color: white;
      text-decoration: none;
      border-radius: 5px;
      transition: background-color 0.3s ease;
    }

    .product-card a:hover {
      background-color: #555;
    }

    footer {
      background-color: #333;
      color: white;
      text-align: center;
      padding: 10px;
      position: fixed;
      bottom: 0;
      width: 100%;
    }
  </style>
</head>
<body>

  <header>
    <h1>Transubtil Rec</h1>
  </header>

  <div class="container">
    <h2>Exclusif products</h2>
    <p>All our merch are unique and exclusif, disponibility may change :</p>

    <!-- Affichage des produits dans une grille -->
    <div class="product-grid">
      <% @products.each do |product| %>
        <div class="product-card">
          <img src="<%= product['image_url'] %>" alt="<%= product['name'] %>">
          <h3><%= product['name'] %></h3>
          <p><%= product['description'] %></p>
          <p class="price"><%= product['price'] %></p>
          <a href="<%= product['link'] %>" target="_blank">Acheter maintenant</a>
        </div>
      <% end %>
    </div>
  </div>

  <footer>
    <p>&copy; 2025 Transubtil Rec - Tous droits réservés</p>
  </footer>

</body>
</html>
